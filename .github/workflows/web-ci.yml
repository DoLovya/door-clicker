name: Web CI

on:
  push:
    branches: [main]
    paths:
      - 'door-clicker-web/**'
      - 'door-clicker-web/requirements.txt'
  pull_request:
    branches: [main]
    paths:
      - 'door-clicker-web/**'
      - 'door-clicker-web/requirements.txt'
  workflow_dispatch:

jobs:
  web-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install flake8
          python -m pip install -r door-clicker-web/requirements.txt

      - name: Run flake8
        run: python -m flake8 door-clicker-web/src/

      - name: Run unit tests
        run: python -m unittest discover -s door-clicker-web/tests -p "*_test.py"
